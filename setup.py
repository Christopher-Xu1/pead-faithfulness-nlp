from setuptools import find_packages, setup

setup(
    name="pead-faithfulness-nlp",
    version="0.1.0",
    description="Faithfulness and interpretability for PEAD prediction from earnings call Q&A",
    packages=find_packages(),
    python_requires=">=3.10",
)
